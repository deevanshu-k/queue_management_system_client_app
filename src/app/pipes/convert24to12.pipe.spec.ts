import { Convert24to12Pipe } from './convert24to12.pipe';

describe('Convert24to12Pipe', () => {
  it('create an instance', () => {
    const pipe = new Convert24to12Pipe();
    expect(pipe).toBeTruthy();
  });
});
